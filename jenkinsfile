pipeline {
    agent any

    environment {
        AWS_DEFAULT_REGION = 'us-west-1'
        TERRAFORM_DIR      = 'terraform'
        LAMBDA_DIR         = 'lambda/nodejs'
        LAMBDA_S3_KEY      = 'lambda/latest.zip'
    }

    stages {

        stage('Checkout Code') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/Pramod581/-Serverless-API-Project.git',
                    credentialsId: 'aws-creds'  // your Jenkins AWS credentials
            }
        }

        stage('Install Node Modules') {
            steps {
                dir("${LAMBDA_DIR}") {
                    sh 'npm install'
                }
            }
        }

        stage('Terraform Init & Apply (Create S3)') {
            steps {
                withCredentials([usernamePassword(
                    credentialsId: 'aws-creds',
                    usernameVariable: 'AWS_ACCESS_KEY_ID',
                    passwordVariable: 'AWS_SECRET_ACCESS_KEY'
                )]) {
                    dir("${TERRAFORM_DIR}") {
                        sh 'terraform init -upgrade'
                        sh 'terraform apply -auto-approve'
                    }
                }
            }
        }

        stage('Zip Lambda') {
            steps {
                dir("${LAMBDA_DIR}") {
                    sh 'zip -r ../../lambda.zip .'
                }
            }
        }

        stage('Upload Lambda to S3') {
            steps {
                withCredentials([usernamePassword(
                    credentialsId: 'aws-creds',
                    usernameVariable: 'AWS_ACCESS_KEY_ID',
                    passwordVariable: 'AWS_SECRET_ACCESS_KEY'
                )]) {
                    script {
                        // Get bucket name from Terraform output
                        def bucket = sh(
                            script: "terraform -chdir=${TERRAFORM_DIR} output -raw s3_bucket",
                            returnStdout: true
                        ).trim()

                        sh "aws s3 cp lambda.zip s3://${bucket}/${LAMBDA_S3_KEY}"
                    }
                }
            }
        }

        stage('Terraform Apply (Update Lambda)') {
            steps {
                withCredentials([usernamePassword(
                    credentialsId: 'aws-creds',
                    usernameVariable: 'AWS_ACCESS_KEY_ID',
                    passwordVariable: 'AWS_SECRET_ACCESS_KEY'
                )]) {
                    dir("${TERRAFORM_DIR}") {
                        sh 'terraform apply -auto-approve'
                    }
                }
            }
        }

        stage('Test API') {
            steps {
                script {
                    def apiUrl = sh(
                        script: "terraform -chdir=${TERRAFORM_DIR} output -raw api_url",
                        returnStdout: true
                    ).trim()

                    echo "GET request..."
                    sh "curl -X GET '${apiUrl}?id=test123' || true"

                    echo "POST request..."
                    sh "curl -X POST '${apiUrl}' -H 'Content-Type: application/json' -d '{\"id\":\"test123\",\"info\":\"Hello\"}'"

                    echo "PUT request..."
                    sh "curl -X PUT '${apiUrl}' -H 'Content-Type: application/json' -d '{\"id\":\"test123\",\"info\":\"Updated\"}'"

                    echo "DELETE request..."
                    sh "curl -X DELETE '${apiUrl}' -H 'Content-Type: application/json' -d '{\"id\":\"test123\"}'"
                }
            }
        }
    }

    post {
        success {
            echo "Deployment Successful! Lambda + API Gateway are Live."
        }
        failure {
            echo "Deployment Failed! Check Pipeline Logs."
        }
    }
}


